# Sprint 20 — data/ 目录分类

**状态**: 🚧 进行中

**目标**：将 `data/` 下的文件按模块分到子目录，便于管理和备份。

## 目录结构

```
data/
  agent/
    memory.json
    news.json
    llm-config.json
    agent-config.json
  im/
    im-config.json
    im-events.json
    conversations.json
  cron/
    cron-config.json
```

## 任务

- [ ] `app.ts`：更新所有文件路径 + mkdirSync 子目录
- [ ] `dev.ts`：同上
- [ ] typecheck + tests 通过
